# SPDX-License-Identifier: MIT
---
- name: "Apply a RHEL Default /etc/sudoers configuration"
  hosts: "all"
  vars:
    sudoers_files:
      - path: "/etc/sudoers"
        user_specifications:
          - users:
            - "root"
            hosts:
              - "x"
            commands:
              - "/usr/bin/ls"
  roles:
    - role: "sudo"
